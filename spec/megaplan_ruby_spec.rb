require 'spec_helper'

describe MegaplanRuby do
  it 'has a version number' do
    expect(MegaplanRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
